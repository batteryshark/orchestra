# Archived screenshots

The images in this directory show a pre-v2 dashboard and are retained only as
visual history. They do not document the Orchestra v2 interface.

Current product behavior is defined by the live responsive dashboard, the
v2 contract fixtures, and the API contract in `docs/API.md`. New screenshots
should be captured from real v2 state at phone, tablet, and desktop widths
before replacing this note.
